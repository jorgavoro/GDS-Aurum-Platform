package com.globaldynamicssystems.aurum.engine.capability.metadata;

public enum CapabilityInputType {
    IDENTIFIER,
    PERIOD,
    ACCOUNT,
    DIMENSION,
    AMOUNT,
    FILTER,
    COLLECTION,
    CONTEXT
}